# 🚀 PHP REST API (CRUD)

A simple **PHP REST API** that demonstrates the basic CRUD operations  
(**Create, Read, Update, Delete**) using plain PHP. 

Unfortunatly the project is incomplete but the code is still there.

This project is intentionally kept minimal to focus on REST principles and core backend logic.

---

## 📌 Features
- Create records (`POST`)
- Read records (`GET`)
- Update records (`PUT`)
- Delete records (`DELETE`)
- JSON request & response handling
- Simple and clear endpoint structure

---

## 🧰 Technology Stack
- PHP (>= 8.0)
- MySQL / MariaDB
- PDO (secure database access)
- RESTful architecture
- JSON

---

## 📂 Project Structure
```text
php-rest-api/
│── public/
│   └── index.php
│── config/
│   └── database.php
│── src/
│   └── ItemController.php
│── .htaccess
